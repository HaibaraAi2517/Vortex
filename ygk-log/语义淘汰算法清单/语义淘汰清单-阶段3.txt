阶段 3 会改到核心内存模型和 HMC 驱逐逻辑，不是小补丁。我先收敛成三条实现路径：namespace quota、pin/chain group
  eviction、redundancy/novelty scoring，然后分层落地并补可验证测试。

• Updated Plan
  └ Stage 3 requires coordinated changes across the fragment model, HMC eviction flow, and API surface. I’m implementing
    quota isolation first, then dependency protection, then redundancy-aware scoring, with tests after each layer.
    □ Design stage 3 model/API additions for quotas, pinning, reasoning groups, and redundancy metadata
    □ Implement namespace hard/soft quota management and quota-aware eviction in HMC/L1 flow
    □ Implement pin TTL, reasoning-chain group protection, and redundancy-aware eviction scoring
    □ Expose new metadata through store APIs and add targeted tests for stage 3 scenarios
    □ Run targeted Maven tests for common, storage, kernel, and app modules

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 我已经把阶段 3 的边界收敛好了：这次会同时改内存元数据、配额控制和淘汰算法，不做“只补一个点”的局部修。接下来先落模型层
  和策略层，再接入 HMC admission 逻辑，最后补 API 和测试。

• Running Get-Content -LiteralPath 'E:
  │ \1projects\claude\Vortex\vortex-kernel\src\main\java\com\vortex\kernel\hmc\EvictionDecisionLogger.java'

• Running Get-Content -LiteralPath 'E:
  │ \1projects\claude\Vortex\vortex-kernel\src\main\java\com\vortex\kernel\hmc\SemanticEvictionPolicy.java'

  │ \1projects\claude\Vortex\vortex-kernel\src\main\java\com\vortex\kernel\hmc\EvictionDecisionLogger.java'
  └ package com.vortex.kernel.hmc;

    … +88 lines (ctrl + t to view transcript)
        }
    }

• Ran Get-Content -LiteralPath 'E:
  │ \1projects\claude\Vortex\vortex-kernel\src\main\java\com\vortex\kernel\hmc\SemanticEvictionPolicy.java'
  └ package com.vortex.kernel.hmc;

    … +103 lines (ctrl + t to view transcript)
        }
    }

• Ran Get-Content -LiteralPath 'E:
  │ \1projects\claude\Vortex\vortex-kernel\src\test\java\com\vortex\kernel\hmc\SemanticEvictionPolicyTest.java'
  └ package com.vortex.kernel.hmc;

    … +95 lines (ctrl + t to view transcript)
        }
    }

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 我现在开始动阶段 3 的核心代码。第一批会加 MemoryFragment 的 pin/chain 元数据、quota 管理器和冗余感知淘汰评分；第二批再
  把 HMC 的 admission/eviction 改成 quota-aware。

• Edited vortex-common\src\main\java\com\vortex\common\model\MemoryFragment.java (+66 -4)
     22      private static final double RECALL_IMPORTANCE_GAIN = 0.15;
     23 +    private static final double REDUNDANCY_THRESHOLD = 0.95;
     24 +    private static final double MAX_REDUNDANCY_PENALTY = 0.25;
     25 +    private static final double MAX_NOVELTY_BONUS = 0.15;
     26
        ⋮
     67
     68 +    /** Optional reasoning chain/group identifier used for dependency-safe retention. */
     69 +    private String reasoningChainId;
     70 +
     71 +    /** Epoch millis until which the fragment is pinned and not eligible for eviction. */
     72 +    private Long pinnedUntil;
     73 +
     74      /**
        ⋮
     78       */
     70 -    public double computeEvictionScore(float[] queryEmbedding, double alpha, double beta, double gamma) {
     71 -        return describeEvictionScore(queryEmbedding, alpha, beta, gamma).totalScore();
     79 +    public double computeEvictionScore(
     80 +            float[] queryEmbedding,
     81 +            double alpha,
     82 +            double beta,
     83 +            double gamma,
     84 +            double redundancyPenalty,
     85 +            double noveltyBonus) {
     86 +        return describeEvictionScore(queryEmbedding, alpha, beta, gamma, redundancyPenalty, noveltyBonus).total
         Score();
     87      }
     88
     74 -    public EvictionScoreBreakdown describeEvictionScore(float[] queryEmbedding, double alpha, double beta, doub
         le gamma) {
     89 +    public EvictionScoreBreakdown describeEvictionScore(
     90 +            float[] queryEmbedding,
     91 +            double alpha,
     92 +            double beta,
     93 +            double gamma,
     94 +            double redundancyPenalty,
     95 +            double noveltyBonus) {
     96          double recency = recencyScore();
        ⋮
    100          double importanceContribution = gamma * importance;
    101 +        double boundedPenalty = clamp(redundancyPenalty, 0.0, MAX_REDUNDANCY_PENALTY);
    102 +        double boundedNovelty = clamp(noveltyBonus, 0.0, MAX_NOVELTY_BONUS);
    103          return new EvictionScoreBreakdown(
        ⋮
    109                  importanceContribution,
     87 -                recencyContribution + similarityContribution + importanceContribution
    110 +                boundedPenalty,
    111 +                boundedNovelty,
    112 +                recencyContribution + similarityContribution + importanceContribution - boundedPenalty + bounde
         dNovelty
    113          );
        ⋮
    130
    131 +    public void pinForMillis(long ttlMillis) {
    132 +        if (ttlMillis <= 0) {
    133 +            return;
    134 +        }
    135 +        pinnedUntil = System.currentTimeMillis() + ttlMillis;
    136 +    }
    137 +
    138 +    public boolean isPinned() {
    139 +        return pinnedUntil != null && pinnedUntil > System.currentTimeMillis();
    140 +    }
    141 +
    142 +    public double redundancySimilarityTo(MemoryFragment other) {
    143 +        if (other == null || other == this) {
    144 +            return 0.0;
    145 +        }
    146 +        return similarityTo(other.getEmbedding());
    147 +    }
    148 +
    149 +    public double redundancyPenaltyAgainst(MemoryFragment other) {
    150 +        double similarity = redundancySimilarityTo(other);
    151 +        if (similarity < REDUNDANCY_THRESHOLD) {
    152 +            return 0.0;
    153 +        }
    154 +        return Math.min(MAX_REDUNDANCY_PENALTY, similarity - REDUNDANCY_THRESHOLD);
    155 +    }
    156 +
    157 +    public double noveltyBonusAgainst(MemoryFragment other) {
    158 +        double similarity = redundancySimilarityTo(other);
    159 +        return Math.min(MAX_NOVELTY_BONUS, Math.max(0.0, 1.0 - similarity) * MAX_NOVELTY_BONUS);
    160 +    }
    161 +
    162      public double similarityTo(float[] queryEmbedding) {
        ⋮
    174              double importanceContribution,
    175 +            double redundancyPenalty,
    176 +            double noveltyBonus,
    177              double totalScore) {
        ⋮
    179
    180 +    private static double clamp(double value, double min, double max) {
    181 +        return Math.max(min, Math.min(max, value));
    182 +    }
    183 +
    184      private static double cosineSimilarity(float[] a, float[] b) {

• Added vortex-kernel\src\main\java\com\vortex\kernel\hmc\NamespaceQuotaManager.java (+95 -0)
     1 +package com.vortex.kernel.hmc;
     2 +
     3 +import com.vortex.common.model.MemoryFragment;
     4 +import org.springframework.beans.factory.annotation.Value;
     5 +import org.springframework.stereotype.Component;
     6 +
     7 +import java.util.Collection;
     8 +import java.util.Comparator;
     9 +import java.util.List;
    10 +import java.util.Map;
    11 +import java.util.Objects;
    12 +import java.util.stream.Collectors;
    13 +
    14 +@Component
    15 +public class NamespaceQuotaManager {
    16 +
    17 +    private final double hardQuotaFraction;
    18 +    private final double softQuotaFraction;
    19 +    private final long minHardQuotaTokens;
    20 +
    21 +    public NamespaceQuotaManager(
    22 +            @Value("${vortex.kernel.namespace-quota.hard-fraction:0.25}") double hardQuotaFraction,
    23 +            @Value("${vortex.kernel.namespace-quota.soft-fraction:0.15}") double softQuotaFraction,
    24 +            @Value("${vortex.kernel.namespace-quota.min-hard-tokens:256}") long minHardQuotaTokens) {
    25 +        this.hardQuotaFraction = hardQuotaFraction;
    26 +        this.softQuotaFraction = softQuotaFraction;
    27 +        this.minHardQuotaTokens = minHardQuotaTokens;
    28 +    }
    29 +
    30 +    public QuotaSnapshot snapshot(Collection<MemoryFragment> fragments, long globalCapacity, String focusNamespa
        ce) {
    31 +        Map<String, Long> namespaceUsage = fragments.stream()
    32 +                .filter(fragment -> fragment.getNamespace() != null && !fragment.getNamespace().isBlank())
    33 +                .collect(Collectors.groupingBy(
    34 +                        MemoryFragment::getNamespace,
    35 +                        Collectors.summingLong(MemoryFragment::getTokenCount)));
    36 +
    37 +        int namespaceCount = Math.max(1, namespaceUsage.size());
    38 +        long defaultHardQuota = Math.max(minHardQuotaTokens, (long) Math.floor(globalCapacity * hardQuotaFractio
        n));
    39 +        long fairShareHardQuota = Math.max(minHardQuotaTokens, globalCapacity / namespaceCount);
    40 +        long hardQuotaPerNamespace = Math.min(defaultHardQuota, fairShareHardQuota);
    41 +        long softQuotaPerNamespace = Math.max(1L, Math.min(hardQuotaPerNamespace, (long) Math.floor(globalCapaci
        ty * softQuotaFraction)));
    42 +
    43 +        long protectedTokens = namespaceUsage.values().stream()
    44 +                .mapToLong(usage -> Math.min(usage, hardQuotaPerNamespace))
    45 +                .sum();
    46 +        long reclaimableBorrowedTokens = namespaceUsage.values().stream()
    47 +                .mapToLong(usage -> Math.max(0L, usage - hardQuotaPerNamespace))
    48 +                .sum();
    49 +        long availableBorrowCapacity = Math.max(0L, globalCapacity - protectedTokens);
    50 +
    51 +        long focusUsage = namespaceUsage.getOrDefault(focusNamespace, 0L);
    52 +        long focusBorrowed = Math.max(0L, focusUsage - hardQuotaPerNamespace);
    53 +        boolean focusAboveSoftQuota = focusUsage > softQuotaPerNamespace;
    54 +        boolean focusAboveHardQuota = focusUsage > hardQuotaPerNamespace;
    55 +
    56 +        return new QuotaSnapshot(
    57 +                globalCapacity,
    58 +                hardQuotaPerNamespace,
    59 +                softQuotaPerNamespace,
    60 +                protectedTokens,
    61 +                availableBorrowCapacity,
    62 +                reclaimableBorrowedTokens,
    63 +                focusUsage,
    64 +                focusBorrowed,
    65 +                focusAboveSoftQuota,
    66 +                focusAboveHardQuota,
    67 +                namespaceUsage
    68 +        );
    69 +    }
    70 +
    71 +    public List<String> evictionPriorityNamespaces(Collection<MemoryFragment> fragments, long globalCapacity, St
        ring focusNamespace) {
    72 +        QuotaSnapshot snapshot = snapshot(fragments, globalCapacity, focusNamespace);
    73 +        return snapshot.namespaceUsage().entrySet().stream()
    74 +                .filter(entry -> !Objects.equals(entry.getKey(), focusNamespace))
    75 +                .sorted(Comparator.<Map.Entry<String, Long>>comparingLong(entry ->
    76 +                                Math.max(0L, entry.getValue() - snapshot.hardQuotaPerNamespace()))
    77 +                        .reversed())
    78 +                .map(Map.Entry::getKey)
    79 +                .toList();
    80 +    }
    81 +
    82 +    public record QuotaSnapshot(
    83 +            long globalCapacity,
    84 +            long hardQuotaPerNamespace,
    85 +            long softQuotaPerNamespace,
    86 +            long protectedTokens,
    87 +            long availableBorrowCapacity,
    88 +            long reclaimableBorrowedTokens,
    89 +            long focusNamespaceUsage,
    90 +            long focusNamespaceBorrowedTokens,
    91 +            boolean focusNamespaceAboveSoftQuota,
    92 +            boolean focusNamespaceAboveHardQuota,
    93 +            Map<String, Long> namespaceUsage) {
    94 +    }
    95 +}

• Edited vortex-kernel\src\main\java\com\vortex\kernel\hmc\SemanticEvictionPolicy.java (+55 -10)
      8  import java.util.ArrayList;
      9 +import java.util.Collection;
     10  import java.util.Comparator;
     11  import java.util.List;
     12 +import java.util.Map;
     13 +import java.util.stream.Collectors;
     14
        ⋮
     59              long targetTokens) {
     57 -        if (targetTokens <= 0 || candidates.isEmpty()) {
     60 +        List<EvictionCandidate> ranked = rankCandidates(candidates, queryEmbedding);
     61 +        if (targetTokens <= 0 || ranked.isEmpty()) {
     62              return List.of();
        ⋮
     64
     61 -        List<EvictionCandidate> ranked = candidates.stream()
     62 -                .map(fragment -> scoreFragment(fragment, queryEmbedding))
     63 -                .sorted(Comparator.comparingDouble(EvictionCandidate::density)
     64 -                        .thenComparingDouble(EvictionCandidate::totalScore))
     65 -                .toList();
     66 -
     65          List<EvictionCandidate> selected = new ArrayList<>();
        ⋮
     76
     77 +    public List<EvictionCandidate> rankCandidates(Collection<MemoryFragment> candidates, float[] queryEmbedding
         ) {
     78 +        if (candidates == null || candidates.isEmpty()) {
     79 +            return List.of();
     80 +        }
     81 +        Map<String, List<MemoryFragment>> reasoningGroups = candidates.stream()
     82 +                .filter(fragment -> fragment.getReasoningChainId() != null && !fragment.getReasoningChainId().i
         sBlank())
     83 +                .collect(Collectors.groupingBy(MemoryFragment::getReasoningChainId));
     84 +
     85 +        return candidates.stream()
     86 +                .map(fragment -> scoreFragment(fragment, queryEmbedding, candidates, reasoningGroups))
     87 +                .filter(candidate -> !candidate.pinned())
     88 +                .sorted(Comparator.comparingDouble(EvictionCandidate::density)
     89 +                        .thenComparingDouble(EvictionCandidate::totalScore))
     90 +                .toList();
     91 +    }
     92 +
     93      public EvictionCandidate scoreFragment(MemoryFragment fragment, float[] queryEmbedding) {
     94 +        return scoreFragment(fragment, queryEmbedding, List.of(fragment), Map.of());
     95 +    }
     96 +
     97 +    private EvictionCandidate scoreFragment(
     98 +            MemoryFragment fragment,
     99 +            float[] queryEmbedding,
    100 +            Collection<MemoryFragment> candidatePool,
    101 +            Map<String, List<MemoryFragment>> reasoningGroups) {
    102 +        double redundancyPenalty = candidatePool.stream()
    103 +                .filter(other -> other != fragment)
    104 +                .mapToDouble(fragment::redundancyPenaltyAgainst)
    105 +                .max()
    106 +                .orElse(0.0);
    107 +        double noveltyBonus = candidatePool.stream()
    108 +                .filter(other -> other != fragment)
    109 +                .mapToDouble(fragment::noveltyBonusAgainst)
    110 +                .min()
    111 +                .orElse(0.15);
    112          MemoryFragment.EvictionScoreBreakdown breakdown =
     81 -                fragment.describeEvictionScore(queryEmbedding, alpha, beta, gamma);
    113 +                fragment.describeEvictionScore(queryEmbedding, alpha, beta, gamma, redundancyPenalty, noveltyBo
         nus);
    114 +        long groupTokenCount = reasoningGroups.getOrDefault(fragment.getReasoningChainId(), List.of(fragment)).
         stream()
    115 +                .mapToLong(MemoryFragment::getTokenCount)
    116 +                .sum();
    117          double density = breakdown.totalScore() / Math.max(1, fragment.getTokenCount());
        ⋮
    125                  breakdown.importanceContribution(),
    126 +                breakdown.redundancyPenalty(),
    127 +                breakdown.noveltyBonus(),
    128                  breakdown.totalScore(),
     92 -                density
    129 +                density,
    130 +                fragment.isPinned(),
    131 +                fragment.getReasoningChainId(),
    132 +                Math.max(fragment.getTokenCount(), groupTokenCount)
    133          );
        ⋮
    143              double importanceContribution,
    144 +            double redundancyPenalty,
    145 +            double noveltyBonus,
    146              double totalScore,
    105 -            double density) {
    147 +            double density,
    148 +            boolean pinned,
    149 +            String reasoningChainId,
    150 +            long groupTokenCount) {
    151      }

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 模型层和评分层已经开始具备阶段 3 所需能力。接下来我把 HMC 改成真正的 quota-aware admission/eviction，同时补 API 入口，
  让外部能设置 pin 和 reasoningChain。

• Edited vortex-kernel\src\main\java\com\vortex\kernel\hmc\EvictionDecisionLogger.java (+15 -0)
     33                  .importanceContribution(candidate.importanceContribution())
     34 +                .redundancyPenalty(candidate.redundancyPenalty())
     35 +                .noveltyBonus(candidate.noveltyBonus())
     36                  .totalScore(candidate.totalScore())
     37                  .scoreDensity(candidate.density())
     38 +                .pinned(candidate.pinned())
     39 +                .reasoningChainId(candidate.reasoningChainId())
     40 +                .groupTokenCount(candidate.groupTokenCount())
     41                  .timestamp(Instant.now())
        ⋮
     62                  .importanceContribution(candidate.importanceContribution())
     63 +                .redundancyPenalty(candidate.redundancyPenalty())
     64 +                .noveltyBonus(candidate.noveltyBonus())
     65                  .totalScore(candidate.totalScore())
     66                  .scoreDensity(candidate.density())
     67 +                .pinned(candidate.pinned())
     68 +                .reasoningChainId(candidate.reasoningChainId())
     69 +                .groupTokenCount(candidate.groupTokenCount())
     70                  .timestamp(Instant.now())
        ⋮
     97              double importanceContribution,
     98 +            double redundancyPenalty,
     99 +            double noveltyBonus,
    100              double totalScore,
    101              double scoreDensity,
    102 +            boolean pinned,
    103 +            String reasoningChainId,
    104 +            long groupTokenCount,
    105              Instant timestamp) {

• Edited vortex-storage\src\main\java\com\vortex\storage\l1\CaffeineHotStore.java (+4 -0)
    88
    89 +    public List<MemoryFragment> getAllFragments() {
    90 +        return List.copyOf(cache.asMap().values());
    91 +    }
    92 +
    93      @Override

• Edited vortex-kernel\src\main\java\com\vortex\kernel\hmc\HierarchicalMemoryController.java (+114 -6)
     39      private final SemanticEvictionPolicy evictionPolicy;
     40 +    private final NamespaceQuotaManager namespaceQuotaManager;
     41      private final EvictionDecisionLogger evictionDecisionLogger;
        ⋮
     63              SemanticEvictionPolicy evictionPolicy,
     64 +            NamespaceQuotaManager namespaceQuotaManager,
     65              EvictionDecisionLogger evictionDecisionLogger,
        ⋮
     75          this.evictionPolicy = evictionPolicy;
     76 +        this.namespaceQuotaManager = namespaceQuotaManager;
     77          this.evictionDecisionLogger = evictionDecisionLogger;
        ⋮
    134          }
    135 +        enforceQuotaBeforeInsert(fragment);
    136          maybeEvict(fragment.getNamespace(), fragment.getEmbedding());
        ⋮
    238          long evictedTokens = 0;
    239 +        Set<String> evictedGroups = new HashSet<>();
    240          for (SemanticEvictionPolicy.EvictionCandidate candidate : toEvict) {
    236 -            MemoryFragment fragment = candidate.fragment();
    237 -            evictionDecisionLogger.logSemanticDecision(candidate, namespace, targetEvict);
    238 -            regretTracker.recordEviction(fragment, "semantic");
    239 -            l1.remove(fragment.getId());
    240 -            persistenceManager.persistAsync(fragment, "semantic-eviction");
    241 -            evictedTokens += fragment.getTokenCount();
    241 +            evictedTokens += evictCandidateGroup(candidate, namespace, targetEvict, "semantic", evictedGroups);
    242              if (evictedTokens >= targetEvict) break;
        ⋮
    260
    261 +    private void enforceQuotaBeforeInsert(MemoryFragment incomingFragment) {
    262 +        if (!(l1 instanceof CaffeineHotStore caffeineStore)) {
    263 +            return;
    264 +        }
    265 +        List<MemoryFragment> allFragments = new ArrayList<>(caffeineStore.getAllFragments());
    266 +        NamespaceQuotaManager.QuotaSnapshot snapshot = namespaceQuotaManager.snapshot(
    267 +                allFragments,
    268 +                l1.maxTokenCapacity(),
    269 +                incomingFragment.getNamespace());
    270 +        long projectedUsage = snapshot.focusNamespaceUsage() + incomingFragment.getTokenCount();
    271 +        if (projectedUsage <= snapshot.hardQuotaPerNamespace()) {
    272 +            return;
    273 +        }
    274 +
    275 +        long requiredTokens = projectedUsage - snapshot.hardQuotaPerNamespace();
    276 +        List<SemanticEvictionPolicy.EvictionCandidate> ownCandidates = evictionPolicy.rankCandidates(
    277 +                l1.getAll(incomingFragment.getNamespace()), incomingFragment.getEmbedding());
    278 +        long released = evictCandidatesUntil(
    279 +                ownCandidates,
    280 +                incomingFragment.getNamespace(),
    281 +                requiredTokens,
    282 +                "quota-self-reclaim");
    283 +        if (released >= requiredTokens) {
    284 +            return;
    285 +        }
    286 +
    287 +        long remainingRequired = requiredTokens - released;
    288 +        for (String otherNamespace : namespaceQuotaManager.evictionPriorityNamespaces(
    289 +                allFragments, l1.maxTokenCapacity(), incomingFragment.getNamespace())) {
    290 +            List<MemoryFragment> namespaceFragments = l1.getAll(otherNamespace);
    291 +            NamespaceQuotaManager.QuotaSnapshot currentSnapshot = namespaceQuotaManager.snapshot(
    292 +                    namespaceFragments,
    293 +                    l1.maxTokenCapacity(),
    294 +                    otherNamespace);
    295 +            long borrowedTokens = Math.max(0L,
    296 +                    namespaceFragments.stream().mapToLong(MemoryFragment::getTokenCount).sum()
    297 +                            - currentSnapshot.hardQuotaPerNamespace());
    298 +            if (borrowedTokens <= 0) {
    299 +                continue;
    300 +            }
    301 +            List<SemanticEvictionPolicy.EvictionCandidate> ranked = evictionPolicy.rankCandidates(
    302 +                    namespaceFragments, incomingFragment.getEmbedding());
    303 +            long evicted = evictCandidatesUntil(
    304 +                    ranked,
    305 +                    otherNamespace,
    306 +                    Math.min(remainingRequired, borrowedTokens),
    307 +                    "quota-borrow-reclaim");
    308 +            remainingRequired -= evicted;
    309 +            if (remainingRequired <= 0) {
    310 +                break;
    311 +            }
    312 +        }
    313 +    }
    314 +
    315 +    private long evictCandidatesUntil(
    316 +            List<SemanticEvictionPolicy.EvictionCandidate> candidates,
    317 +            String triggerNamespace,
    318 +            long targetTokens,
    319 +            String reason) {
    320 +        Set<String> evictedGroups = new HashSet<>();
    321 +        long evictedTokens = 0;
    322 +        for (SemanticEvictionPolicy.EvictionCandidate candidate : candidates) {
    323 +            evictedTokens += evictCandidateGroup(candidate, triggerNamespace, targetTokens, reason, evictedGrou
         ps);
    324 +            if (evictedTokens >= targetTokens) {
    325 +                break;
    326 +            }
    327 +        }
    328 +        return evictedTokens;
    329 +    }
    330 +
    331 +    private long evictCandidateGroup(
    332 +            SemanticEvictionPolicy.EvictionCandidate candidate,
    333 +            String triggerNamespace,
    334 +            long targetTokens,
    335 +            String reason,
    336 +            Set<String> evictedGroups) {
    337 +        if (candidate.pinned()) {
    338 +            return 0;
    339 +        }
    340 +        String groupId = candidate.reasoningChainId();
    341 +        if (groupId != null && !groupId.isBlank() && !evictedGroups.add(groupId)) {
    342 +            return 0;
    343 +        }
    344 +
    345 +        List<MemoryFragment> evictionGroup = resolveEvictionGroup(candidate);
    346 +        long released = 0;
    347 +        for (MemoryFragment fragment : evictionGroup) {
    348 +            SemanticEvictionPolicy.EvictionCandidate scored = evictionPolicy.scoreFragment(fragment, candidate.
         fragment().getEmbedding());
    349 +            evictionDecisionLogger.logSemanticDecision(scored, triggerNamespace, targetTokens);
    350 +            regretTracker.recordEviction(fragment, reason);
    351 +            l1.remove(fragment.getId());
    352 +            persistenceManager.persistAsync(fragment, reason);
    353 +            released += fragment.getTokenCount();
    354 +        }
    355 +        return released;
    356 +    }
    357 +
    358 +    private List<MemoryFragment> resolveEvictionGroup(SemanticEvictionPolicy.EvictionCandidate candidate) {
    359 +        String groupId = candidate.reasoningChainId();
    360 +        if (groupId == null || groupId.isBlank()) {
    361 +            return List.of(candidate.fragment());
    362 +        }
    363 +        return l1.getAll(candidate.fragment().getNamespace()).stream()
    364 +                .filter(fragment -> groupId.equals(fragment.getReasoningChainId()))
    365 +                .filter(fragment -> !fragment.isPinned())
    366 +                .toList();
    367 +    }
    368 +
    369      private void handleCaffeineEviction(MemoryFragment fragment, RemovalCause cause) {

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• HMC 已经开始具备 quota-aware 和 group-aware 驱逐能力，但测试和 API 还没跟上。我接下来补外部接口和阶段 3 核心回归用例，
  重点验证三件事：A 写爆不清空 B 的核心配额、pinned 片段不会被淘汰、同链路片段组一起淘汰。

• Edited 3 files (+35 -10)
  └ vortex-app\src\main\java\com\vortex\app\controller\MemoryController.java (+7 -2)
    33      public ResponseEntity<Map<String, Object>> store(@RequestBody StoreRequest req) {
    34 -        List<String> ids = hmc.store(req.content(), req.namespace(), req.tags());
    34 +        List<String> ids = hmc.store(req.content(), req.namespace(), req.tags(), req.reasoningChainId(), req.pin
        TtlMillis());
    35          return ResponseEntity.ok(Map.of(
       ⋮
    80
    81 -    public record StoreRequest(String content, String namespace, List<String> tags) {}
    81 +    public record StoreRequest(
    82 +            String content,
    83 +            String namespace,
    84 +            List<String> tags,
    85 +            String reasoningChainId,
    86 +            Long pinTtlMillis) {}
    87  }

  └ vortex-kernel\src\main\java\com\vortex\kernel\hmc\HierarchicalMemoryController.java (+7 -2)
    110       */
    111 -    public List<String> store(String content, String namespace, List<String> tags) {
    112 -        List<MemoryFragment> chunks = splitter.split(content, namespace, tags);
    111 +    public List<String> store(
    112 +            String content,
    113 +            String namespace,
    114 +            List<String> tags,
    115 +            String reasoningChainId,
    116 +            Long pinTtlMillis) {
    117 +        List<MemoryFragment> chunks = splitter.split(content, namespace, tags, reasoningChainId, pinTtlMillis);
    118          List<String> ids = new ArrayList<>();

  └ vortex-kernel\src\main\java\com\vortex\kernel\hmc\SemanticTextSplitter.java (+21 -6)
     46       */
     47 -    public List<MemoryFragment> split(String text, String namespace, List<String> tags) {
     47 +    public List<MemoryFragment> split(
     48 +            String text,
     49 +            String namespace,
     50 +            List<String> tags,
     51 +            String reasoningChainId,
     52 +            Long pinTtlMillis) {
     53          List<MemoryFragment> result = new ArrayList<>();
        ⋮
     59              if (tokens <= maxTokensPerChunk) {
     55 -                result.add(buildFragment(trimmed, namespace, tags));
     60 +                result.add(buildFragment(trimmed, namespace, tags, reasoningChainId, pinTtlMillis));
     61              } else {
        ⋮
     68                      if (bufTokens + st > maxTokensPerChunk && buf.length() > 0) {
     64 -                        result.add(buildFragment(buf.toString().strip(), namespace, tags));
     69 +                        result.add(buildFragment(buf.toString().strip(), namespace, tags, reasoningChainId, pin
         TtlMillis));
     70                          buf.setLength(0);
        ⋮
     76                  if (buf.length() > 0) {
     72 -                    result.add(buildFragment(buf.toString().strip(), namespace, tags));
     77 +                    result.add(buildFragment(buf.toString().strip(), namespace, tags, reasoningChainId, pinTtlM
         illis));
     78                  }
        ⋮
     83
     79 -    private MemoryFragment buildFragment(String content, String namespace, List<String> tags) {
     80 -        return MemoryFragment.builder()
     84 +    private MemoryFragment buildFragment(
     85 +            String content,
     86 +            String namespace,
     87 +            List<String> tags,
     88 +            String reasoningChainId,
     89 +            Long pinTtlMillis) {
     90 +        MemoryFragment fragment = MemoryFragment.builder()
     91                  .id(UUID.randomUUID().toString())
        ⋮
     95                  .tags(tags == null ? List.of() : List.copyOf(tags))
     96 +                .reasoningChainId(reasoningChainId)
     97                  .build();
     98 +        if (pinTtlMillis != null) {
     99 +            fragment.pinForMillis(pinTtlMillis);
    100 +        }
    101 +        return fragment;
    102      }

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• API 入口已经补到位。现在我补阶段 3 的测试并修正现有构造器/调用方，确保新签名和新策略都有覆盖。之后直接跑跨模块回归。

• Edited 3 files (+192 -1)
  └ vortex-kernel\src\test\java\com\vortex\kernel\hmc\HierarchicalMemoryControllerTest.java (+104 -0)
     42                  new SemanticEvictionPolicy(0.3, 0.5, 0.2),
     43 +                new NamespaceQuotaManager(0.25, 0.15, 16),
     44                  new EvictionDecisionLogger(),
        ⋮
     67                  new SemanticEvictionPolicy(0.3, 0.5, 0.2),
     68 +                new NamespaceQuotaManager(0.25, 0.15, 16),
     69                  new EvictionDecisionLogger(),
        ⋮
    119                  new SemanticEvictionPolicy(0.3, 0.5, 0.2),
    120 +                new NamespaceQuotaManager(0.25, 0.15, 16),
    121                  new EvictionDecisionLogger(),
        ⋮
    160                  new SemanticEvictionPolicy(0.0, 0.0, 1.0),
    161 +                new NamespaceQuotaManager(0.25, 0.15, 16),
    162                  new EvictionDecisionLogger(),
        ⋮
    198                  new SemanticEvictionPolicy(0.3, 0.5, 0.2),
    199 +                new NamespaceQuotaManager(0.25, 0.15, 16),
    200                  new EvictionDecisionLogger(),
        ⋮
    224
    225 +    @Test
    226 +    void pinnedFragmentsAreNotEvictedAndReasoningChainEvictsAsGroup() {
    227 +        CaffeineHotStore l1 = new CaffeineHotStore(100);
    228 +        FakeL2WarmStore l2 = new FakeL2WarmStore(4);
    229 +        FakeL3ColdStore l3 = new FakeL3ColdStore();
    230 +        EmbeddingService localEmbedding = new FixedEmbeddingService(4);
    231 +
    232 +        HierarchicalMemoryController hmc = new HierarchicalMemoryController(
    233 +                l1,
    234 +                l2,
    235 +                l3,
    236 +                new SemanticEvictionPolicy(0.0, 0.0, 1.0),
    237 +                new NamespaceQuotaManager(0.25, 0.15, 16),
    238 +                new EvictionDecisionLogger(),
    239 +                new EvictionRegretTracker(3_600_000L, System::currentTimeMillis),
    240 +                persistenceManager(l2, l3),
    241 +                new SemanticTextSplitter(text -> Math.max(1, text.length()), 64),
    242 +                localEmbedding,
    243 +                emptyProvider(),
    244 +                0.5
    245 +        );
    246 +
    247 +        MemoryFragment pinned = fragment("pinned", "ns", "pinned", List.of(), 4);
    248 +        pinned.setTokenCount(20);
    249 +        pinned.pinForMillis(60_000L);
    250 +        MemoryFragment chainA = fragment("chain-a", "ns", "chain-a", List.of(), 4);
    251 +        chainA.setTokenCount(20);
    252 +        chainA.setReasoningChainId("chain-1");
    253 +        chainA.setImportance(0.1);
    254 +        MemoryFragment chainB = fragment("chain-b", "ns", "chain-b", List.of(), 4);
    255 +        chainB.setTokenCount(20);
    256 +        chainB.setReasoningChainId("chain-1");
    257 +        chainB.setImportance(0.1);
    258 +        MemoryFragment survivor = fragment("survivor", "ns", "survivor", List.of(), 4);
    259 +        survivor.setTokenCount(20);
    260 +        survivor.setImportance(0.9);
    261 +
    262 +        l1.put(pinned);
    263 +        l1.put(chainA);
    264 +        l1.put(chainB);
    265 +        l1.put(survivor);
    266 +
    267 +        hmc.maybeEvict("ns", vector(4));
    268 +
    269 +        assertThat(l1.get("pinned")).isPresent();
    270 +        assertThat(l1.get("chain-a")).isEmpty();
    271 +        assertThat(l1.get("chain-b")).isEmpty();
    272 +        assertThat(l1.get("survivor")).isPresent();
    273 +    }
    274 +
    275 +    @Test
    276 +    void quotaReclaimsBorrowedCapacityWithoutClearingOtherNamespaceCoreSet() {
    277 +        CaffeineHotStore l1 = new CaffeineHotStore(120);
    278 +        FakeL2WarmStore l2 = new FakeL2WarmStore(4);
    279 +        FakeL3ColdStore l3 = new FakeL3ColdStore();
    280 +        EmbeddingService localEmbedding = new FixedEmbeddingService(4);
    281 +
    282 +        HierarchicalMemoryController hmc = new HierarchicalMemoryController(
    283 +                l1,
    284 +                l2,
    285 +                l3,
    286 +                new SemanticEvictionPolicy(0.0, 0.0, 1.0),
    287 +                new NamespaceQuotaManager(0.25, 0.15, 20),
    288 +                new EvictionDecisionLogger(),
    289 +                new EvictionRegretTracker(3_600_000L, System::currentTimeMillis),
    290 +                persistenceManager(l2, l3),
    291 +                new SemanticTextSplitter(text -> Math.max(1, text.length()), 64),
    292 +                localEmbedding,
    293 +                emptyProvider(),
    294 +                0.95
    295 +        );
    296 +
    297 +        MemoryFragment coreB = fragment("b-core", "ns-b", "b-core", List.of(), 4);
    298 +        coreB.setTokenCount(20);
    299 +        coreB.setImportance(0.9);
    300 +        MemoryFragment borrowedB = fragment("b-borrowed", "ns-b", "b-borrowed", List.of(), 4);
    301 +        borrowedB.setTokenCount(20);
    302 +        borrowedB.setImportance(0.1);
    303 +        MemoryFragment a1 = fragment("a1", "ns-a", "a1", List.of(), 4);
    304 +        a1.setTokenCount(40);
    305 +        a1.setImportance(0.2);
    306 +        MemoryFragment a2 = fragment("a2", "ns-a", "a2", List.of(), 4);
    307 +        a2.setTokenCount(40);
    308 +        a2.setImportance(0.2);
    309 +
    310 +        l1.put(coreB);
    311 +        l1.put(borrowedB);
    312 +        l1.put(a1);
    313 +        l1.put(a2);
    314 +
    315 +        MemoryFragment incomingA = fragment("a3", "ns-a", "a3", List.of(), 4);
    316 +        incomingA.setTokenCount(30);
    317 +        incomingA.setImportance(0.2);
    318 +        hmc.storeFragment(incomingA);
    319 +
    320 +        assertThat(l1.get("b-core")).isPresent();
    321 +        assertThat(l1.get("a3")).isPresent();
    322 +    }
    323 +
    324      private static MemoryFragment fragment(String id, String namespace, String content, List<String> tags, int
         dim) {

  └ vortex-kernel\src\test\java\com\vortex\kernel\hmc\NamespaceQuotaManagerTest.java (+49 -0)
     1 +package com.vortex.kernel.hmc;
     2 +
     3 +import com.vortex.common.model.MemoryFragment;
     4 +import org.junit.jupiter.api.Test;
     5 +
     6 +import java.util.List;
     7 +
     8 +import static org.assertj.core.api.Assertions.assertThat;
     9 +
    10 +class NamespaceQuotaManagerTest {
    11 +
    12 +    @Test
    13 +    void snapshotProtectsHardQuotaForEachNamespace() {
    14 +        NamespaceQuotaManager manager = new NamespaceQuotaManager(0.25, 0.15, 20);
    15 +
    16 +        NamespaceQuotaManager.QuotaSnapshot snapshot = manager.snapshot(List.of(
    17 +                fragment("a1", "a", 40),
    18 +                fragment("b1", "b", 20),
    19 +                fragment("b2", "b", 20)
    20 +        ), 120, "a");
    21 +
    22 +        assertThat(snapshot.hardQuotaPerNamespace()).isEqualTo(30);
    23 +        assertThat(snapshot.softQuotaPerNamespace()).isEqualTo(18);
    24 +        assertThat(snapshot.focusNamespaceBorrowedTokens()).isEqualTo(10);
    25 +        assertThat(snapshot.reclaimableBorrowedTokens()).isEqualTo(10);
    26 +    }
    27 +
    28 +    @Test
    29 +    void evictionPriorityOrdersNamespacesByBorrowedUsage() {
    30 +        NamespaceQuotaManager manager = new NamespaceQuotaManager(0.25, 0.15, 20);
    31 +
    32 +        List<String> priorities = manager.evictionPriorityNamespaces(List.of(
    33 +                fragment("a1", "a", 40),
    34 +                fragment("b1", "b", 35),
    35 +                fragment("c1", "c", 20)
    36 +        ), 120, "a");
    37 +
    38 +        assertThat(priorities).containsExactly("b", "c");
    39 +    }
    40 +
    41 +    private static MemoryFragment fragment(String id, String namespace, int tokens) {
    42 +        return MemoryFragment.builder()
    43 +                .id(id)
    44 +                .namespace(namespace)
    45 +                .content(id)
    46 +                .tokenCount(tokens)
    47 +                .build();
    48 +    }
    49 +}

  └ vortex-kernel\src\test\java\com\vortex\kernel\hmc\SemanticEvictionPolicyTest.java (+39 -1)
     93          assertThat(candidate.importanceContribution()).isEqualTo(0.1);
     94 +        assertThat(candidate.noveltyBonus()).isGreaterThanOrEqualTo(0.0);
     95          assertThat(candidate.totalScore()).isEqualTo(
        ⋮
     97                          + candidate.similarityContribution()
     97 -                        + candidate.importanceContribution());
     98 +                        + candidate.importanceContribution()
     99 +                        - candidate.redundancyPenalty()
    100 +                        + candidate.noveltyBonus());
    101 +    }
    102 +
    103 +    @Test
    104 +    void rankCandidates_skipsPinnedFragmentsAndPenalizesRedundantCopies() {
    105 +        MemoryFragment pinned = MemoryFragment.builder()
    106 +                .id("pinned")
    107 +                .content("pinned")
    108 +                .embedding(new float[]{1.0f, 0.0f})
    109 +                .tokenCount(10)
    110 +                .importance(0.1)
    111 +                .build();
    112 +        pinned.pinForMillis(60_000L);
    113 +        MemoryFragment redundantA = MemoryFragment.builder()
    114 +                .id("dup-a")
    115 +                .content("dup-a")
    116 +                .embedding(new float[]{1.0f, 0.0f})
    117 +                .tokenCount(10)
    118 +                .importance(0.1)
    122 +                .content("dup-b")
    123 +                .embedding(new float[]{1.0f, 0.0f})
    124 +                .tokenCount(10)
    125 +                .importance(0.1)
    126 +                .build();
    127 +
    128 +        List<SemanticEvictionPolicy.EvictionCandidate> ranked = policy.rankCandidates(
    129 +                List.of(pinned, redundantA, redundantB),
    130 +                new float[]{0.0f, 1.0f});
    131 +
    132 +        assertThat(ranked)
    133 +                .extracting(candidate -> candidate.fragment().getId())
    134 +                .doesNotContain("pinned");
    135 +        assertThat(ranked).allMatch(candidate -> candidate.redundancyPenalty() > 0.0);
    136      }

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 阶段 3 的核心行为测试已经补上，但现有代码现在很可能还有编译面的小问题，尤其是 HMC 新签名和 store() 调用点。我先做一次
  全文搜索和静态自检，再跑 Maven。

• Running rg --line-number "\.store\(|new HierarchicalMemoryController\(|describeEvictionScore\(|computeEvictionScore\("
  │ vortex-app\src\main\java vortex-kernel\src\main\java vortex-kernel\src\test\java vortex-common\src\main\java
  │ vortex-common\src\test\java

• Running Get-Content -LiteralPath 'E:
  │ \1projects\claude\Vortex\vortex-common\src\main\java\com\vortex\common\model\MemoryFragment.java'

• Ran rg --line-number "\.store\(|new HierarchicalMemoryController\(|describeEvictionScore\(|computeEvictionScore\("
  │ vortex-app\src\main\java vortex-kernel\src\main\java vortex-kernel\src\test\java vortex-common\src\main\java
  │ vortex-common\src\test\java
  └ vortex-common\src\test\java\com\vortex\common\model\MemoryFragmentTest.java:26:        double twoDaysScore =
    twoDaysOld.computeEvictionScore(null, 1.0, 0.0, 0.0);
    … +19 lines (ctrl + t to view transcript)
    vortex-kernel\src\main\java\com\vortex\kernel\hmc\SemanticEvictionPolicy.java:113:
    fragment.describeEvictionScore(queryEmbedding, alpha, beta, gamma, redundancyPenalty, noveltyBonus);

• Ran Get-Content -LiteralPath 'E:
  │ \1projects\claude\Vortex\vortex-kernel\src\main\java\com\vortex\kernel\hmc\HierarchicalMemoryController.java'
  └ package com.vortex.kernel.hmc;

    … +447 lines (ctrl + t to view transcript)
        }
    }

• Ran Get-Content -LiteralPath 'E:
  │ \1projects\claude\Vortex\vortex-common\src\main\java\com\vortex\common\model\MemoryFragment.java'
  └ package com.vortex.common.model;

    … +191 lines (ctrl + t to view transcript)
        }
    }

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 我已经发现一个必修补点：MemoryFragmentTest 还在用旧的 computeEvictionScore 签名，阶段 3 改完后会直接编译失败。接下来我
  统一修测试与少量逻辑细节，再跑全套定向验证。

  └ vortex-kernel\src\main\java\com\vortex\kernel\hmc\NamespaceQuotaManager.java (+2 -1)
    38          long defaultHardQuota = Math.max(minHardQuotaTokens, (long) Math.floor(globalCapacity * hardQuotaFractio
        n));
    39 -        long fairShareHardQuota = Math.max(minHardQuotaTokens, globalCapacity / namespaceCount);
    39 +        long fairShareHardQuota = Math.max(1L, globalCapacity / namespaceCount);
    74                  .filter(entry -> !Objects.equals(entry.getKey(), focusNamespace))
    75 +                .filter(entry -> entry.getValue() > snapshot.hardQuotaPerNamespace())
    76                  .sorted(Comparator.<Map.Entry<String, Long>>comparingLong(entry ->

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 我已经把明显的签名回归和 quota 计算边界修掉了。现在开始跑阶段 3 的定向测试，先看编译和核心行为，再根据失败点继续收敛。

• Ran mvn test -pl vortex-common,vortex-storage,vortex-kernel,vortex-app -am
  │ "-Dtest=MemoryFragmentTest,CaffeineHotStoreTest,SemanticEvictionPolicyTest,NamespaceQuotaManagerTest,HierarchicalM
  │ emoryControllerTest,FragmentPersistenceManagerTest,EvictionRegretTrackerTest,EvictionRegretMetricsBinderTest"
  └ [INFO] Scanning for projects...
    [INFO] ------------------------------------------------------------------------
    … +202 lines (ctrl + t to view transcript)
    [ERROR] After correcting the problems, you can resume the build with the command
    [ERROR]   mvn <args> -rf :vortex-kernel

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 当前失败点很清楚：不是阶段 3 逻辑本身，而是 SemanticTextSplitterTest 还没适配新的 split(...) 签名。我先补这组测试调
  用，再继续跑。

• Running Get-Content -LiteralPath 'E:
  │ \1projects\claude\Vortex\vortex-kernel\src\test\java\com\vortex\kernel\hmc\SemanticTextSplitterTest.java'

• Ran rg --line-number "split\(" vortex-kernel\src\test\java\com\vortex\kernel\hmc
  └ vortex-kernel\src\test\java\com\vortex\kernel\hmc\SemanticTextSplitterTest.java:13:            text -> text == null
    || text.isBlank() ? 0 : text.trim().split("\\s+").length,
    … +6 lines (ctrl + t to view transcript)
    vortex-kernel\src\test\java\com\vortex\kernel\hmc\SemanticTextSplitterTest.java:40:        List<MemoryFragment>
    result = splitter.split("Token counting test.", "ns", List.of());

• Ran Get-Content -LiteralPath 'E:
  │ \1projects\claude\Vortex\vortex-kernel\src\test\java\com\vortex\kernel\hmc\SemanticTextSplitterTest.java'
  └ package com.vortex.kernel.hmc;

    … +44 lines (ctrl + t to view transcript)
        }
    }

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 我现在只修这组测试的签名调用，顺便补一个断言，确保 reasoningChainId 和 pinTtlMillis 能从 splitter 透传到 fragment。然
  后继续跑完整回归。

• Edited vortex-kernel\src\test\java\com\vortex\kernel\hmc\SemanticTextSplitterTest.java (+6 -4)
    18      void split_shortText_returnsSingleFragment() {
    19 -        List<MemoryFragment> result = splitter.split("Hello world.", "ns", List.of());
    19 +        List<MemoryFragment> result = splitter.split("Hello world.", "ns", List.of(), null, null);
    20          assertThat(result).hasSize(1);
       ⋮
    26          String text = "First paragraph.\n\nSecond paragraph.\n\nThird paragraph.";
    37 +        assertThat(result).allMatch(MemoryFragment::isPinned);
    38      }
       ⋮
    41      void split_tokenCountIsPositive() {
    40 -        List<MemoryFragment> result = splitter.split("Token counting test.", "ns", List.of());
    42 +        List<MemoryFragment> result = splitter.split("Token counting test.", "ns", List.of(), null, null);
    43          assertThat(result).allMatch(f -> f.getTokenCount() > 0);

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 我继续跑阶段 3 回归。现在主要看两类问题：新的 quota/group 逻辑是否通过测试，以及 app 模块是否因为 store API 变更出现编
  译面回归。

• Ran mvn test -pl vortex-common,vortex-storage,vortex-kernel,vortex-app -am
  │ "-Dtest=MemoryFragmentTest,CaffeineHotStoreTest,SemanticTextSplitterTest,SemanticEvictionPolicyTest,NamespaceQuota
  │ ManagerTest,HierarchicalMemoryControllerTest,FragmentPersistenceManagerTest,EvictionRegretTrackerTest,EvictionRegr
  │ … +1 lines
  └ [INFO] Scanning for projects...
    [INFO] ------------------------------------------------------------------------
    … +280 lines (ctrl + t to view transcript)
    [ERROR] After correcting the problems, you can resume the build with the command
    [ERROR]   mvn <args> -rf :vortex-kernel

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 我已经拿到失败面了，都是可修的实现/测试偏差，不是方向错了。下一轮我会处理 4 个点：SemanticEvictionPolicy 的空 group 键
  NPE、MemoryFragment 的 pinned JSON 序列化污染、quota 测试基线修正，以及回归后再跑一遍完整阶段 3。

• Edited 3 files (+8 -5)
  └ vortex-common\src\main\java\com\vortex\common\model\MemoryFragment.java (+2 -0)
      2
      3 +import com.fasterxml.jackson.annotation.JsonIgnore;
      4  import lombok.AllArgsConstructor;
        ⋮
    138
    139 +    @JsonIgnore
    140      public boolean isPinned() {

  └ vortex-kernel\src\main\java\com\vortex\kernel\hmc\SemanticEvictionPolicy.java (+4 -3)
    113                  fragment.describeEvictionScore(queryEmbedding, alpha, beta, gamma, redundancyPenalty, noveltyBo
         nus);
    114 -        long groupTokenCount = reasoningGroups.getOrDefault(fragment.getReasoningChainId(), List.of(fragment)).
         stream()
    115 -                .mapToLong(MemoryFragment::getTokenCount)
    25 -        assertThat(snapshot.reclaimableBorrowedTokens()).isEqualTo(10);
    25 +        assertThat(snapshot.reclaimableBorrowedTokens()).isEqualTo(20);
    26      }
       ⋮
    37
  │ "-Dtest=MemoryFragmentTest,CaffeineHotStoreTest,SemanticTextSplitterTest,SemanticEvictionPolicyTest,NamespaceQuota
  │ ManagerTest,HierarchicalMemoryControllerTest,FragmentPersistenceManagerTest,EvictionRegretTrackerTest,EvictionRegr
  │ … +1 lines
  └ [INFO] Scanning for projects...
• Ran mvn test -pl vortex-common,vortex-storage,vortex-kernel,vortex-app -am -Dsurefire.failIfNoSpecifiedTests=false
  │ "-Dtest=MemoryFragmentTest,CaffeineHotStoreTest,SemanticTextSplitterTest,SemanticEvictionPolicyTest,NamespaceQuota
  │ ManagerTest,HierarchicalMemoryControllerTest,FragmentPersistenceManagerTest,EvictionRegretTrackerTest,EvictionRegr
  │ … +1 lines
  └ [INFO] Scanning for projects...
    [INFO] ------------------------------------------------------------------------
    … +31 lines (ctrl + t to view transcript)
    [ERROR] For more information about the errors and possible solutions, please read the following articles:
    [ERROR] [Help 1] http://cwiki.apache.org/confluence/display/MAVEN/LifecyclePhaseNotFoundException

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 这是 PowerShell 参数解析问题，-D... 需要整体加引号。我只重跑同一条命令的正确形式，完成最终验证。

• Ran mvn test -pl vortex-common,vortex-storage,vortex-kernel,vortex-app -am "-Dsurefire.failIfNoSpecifiedTests=false"
  │ "-Dtest=MemoryFragmentTest,CaffeineHotStoreTest,SemanticTextSplitterTest,SemanticEvictionPolicyTest,NamespaceQuota
  │ ManagerTest,HierarchicalMemoryControllerTest,FragmentPersistenceManagerTest,EvictionRegretTrackerTest,EvictionRegr
  │ … +1 lines
  └ [INFO] Scanning for projects...
    [INFO] ------------------------------------------------------------------------
    … +165 lines (ctrl + t to view transcript)
    [INFO] Finished at: 2026-05-09T16:51:21+08:00
    [INFO] ------------------------------------------------------------------------

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 阶段 3 已完成并通过定向验证。

  这次落实了三块核心能力：

  - namespace 强隔离配额：新增 /E:/1projects/claude/Vortex/vortex-kernel/src/main/java/com/vortex/kernel/hmc/
    NamespaceQuotaManager.java，HMC 在写入前会先做 hardQuota/softQuota 判定，优先回收本 namespace 超额部分，再回收其他
    namespace 的“借用容量”，不会清空别人的核心热数据。
  - pin 与组淘汰：MemoryFragment 新增 pinnedUntil、reasoningChainId，位置在 /E:/1projects/claude/Vortex/vortex-common/
    src/main/java/com/vortex/common/model/MemoryFragment.java。驱逐时会跳过 pinned 片段，并对同一 reasoningChainId 组做
    一致性淘汰，入口在 /E:/1projects/claude/Vortex/vortex-kernel/src/main/java/com/vortex/kernel/hmc/
    HierarchicalMemoryController.java。
  - 冗余度维度：SemanticEvictionPolicy 现在会感知高相似重复片段的冗余惩罚和新颖性加分，位置在 /E:/1projects/claude/
    Vortex/vortex-kernel/src/main/java/com/vortex/kernel/hmc/SemanticEvictionPolicy.java。

  API 也补上了：

  - /api/v1/memory/store 现在支持 reasoningChainId 和 pinTtlMillis，位置在 /E:/1projects/claude/Vortex/vortex-app/src/
    main/java/com/vortex/app/controller/MemoryController.java

  验证已通过：

  - mvn test -pl vortex-common,vortex-storage,vortex-kernel,vortex-app -am "-Dsurefire.failIfNoSpecifiedTests=false"
    "-Dtest=MemoryFragmentTest,CaffeineHotStoreTest,SemanticTextSplitterTest,SemanticEvictionPolicyTest,NamespaceQuotaMa
    nagerTest,HierarchicalMemoryControllerTest,FragmentPersistenceManagerTest,EvictionRegretTrackerTest,EvictionRegretMe
    tricsBinderTest"

  补充说明：

  - 现在的 quota 是“全局容量按 namespace 数量动态公平切分 + hard protected core + soft borrow”，已经满足阶段 3 清单的隔
    离目标。
  - reasoningChain 组淘汰是按同 namespace 内同组一致处理，不会拆散链路。
  - 冗余度当前基于 embedding cosine 做在线判定，阈值逻辑已经内置，但还没做持久层合并压缩，这一步更适合放到后续阶段 4 或
    单独优化。

  如果继续，我下一步进入阶段 4。