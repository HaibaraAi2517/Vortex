阶段2总体结论
  阶段2：2.5 / 3 项达标（工程实现层面很强）
  - #6 达标
  - #7 基本达标，但“监控闭环”还差最后一跳
  - #8 基本达标，但“严格幂等语义”还需存储层配合

  ---
  6) 写入 eviction decision log

  结论：✅ 已完成（质量高）

  证据

  - 结构化日志组件已实现：
  vortex-kernel/src/main/java/com/vortex/kernel/hmc/EvictionDecisionLogger.java:12-90
  - 字段覆盖完整：fragmentId / fragmentNamespace / triggerNamespace / mode / tokenCount / recency/similarity/importance
  分项与贡献 / totalScore / density / timestamp
  见 EvictionDecisionLogger.java:21-35, 74-89
  - 语义淘汰路径已打点：
  HierarchicalMemoryController.java:236-239
  - Caffeine fallback 淘汰路径已打点：
  HierarchicalMemoryController.java:260-265

  评价：这项实现已经满足你清单里“可追溯决策”的核心要求。

  ---
  7) 统计 Eviction Regret 指标

  结论：🟡 已完成核心算法与埋点，监控暴露层部分缺失

  已完成部分

  - Regret 窗口、计数、比率计算都有：
  EvictionRegretTracker.java:18-23, 75-81, 101-107
  - TTL 窗口逻辑正确（过窗不计 regret）：
  EvictionRegretTracker.java:57-60, 83-92
  - 仅 L2/L3 类回补才算 regret（L1 不算）：
  EvictionRegretTracker.java:45-48
  - HMC 已接入：
    - 淘汰时记录：HierarchicalMemoryController.java:237, 263
    - L2召回时回记 regret：HierarchicalMemoryController.java:198
  - 单测覆盖存在：
  EvictionRegretTrackerTest.java:12-48，HierarchicalMemoryControllerTest.java:183-218

  缺口（你若按“阶段2完全验收”）

  - 目前 snapshot() 只在测试里使用，生产代码里未见指标导出或 API 暴露：
  搜索结果显示生产侧无 .snapshot() 调用。
  - 也就是说：“能算 regret”已完成，但“可持续监控 regret rate <5%”还没闭环。

  ---
  8) 下沉链路幂等 & 可重放

  结论：🟡 可靠性机制已落地，幂等保障是“弱幂等/依赖下层upsert覆盖”

  已完成部分

  - 异步下沉管理器已实现：
  FragmentPersistenceManager.java:18-105
  - 失败入 DLQ：
  FragmentPersistenceManager.java:65-78
  - 启动自动回放：
  FragmentPersistenceManager.java:37-42, 54-63
  - 文件型 DLQ（JSONL）与失败重写保留：
  FileBackedDeadLetterQueue.java:33-66, 91-103
  - 幂等键已生成并随任务携带：
  FragmentPersistenceTask.java:16，FragmentPersistenceManager.java:93-99
  - 单测验证“失败→入队→回放成功”：
  FragmentPersistenceManagerTest.java:23-54

  关键提醒（专家视角）

  - 当前 idempotencyKey
  主要用于日志与任务标识，未看到对“重复任务去重”的硬性检查（例如已处理键存储、幂等表、或下层条件写）。
  - 但你 L2 用的是 upsert，L3 用固定 key 覆盖写（fragments/{id}.json），在当前模型下具备一定“结果幂等性”。
    - L2: MilvusWarmStore.java:121-146
    - L3: MinioColdStore.java:66-69
  - 所以这项我给“可靠性完成，严格幂等语义接近完成”。

  ---
  最终判定（阶段2）

  如果你定义“阶段2完成”是代码机制已具备并可运行，我认可你“已完成阶段2”的说法。
  如果你定义“阶段2完成”是达到可运营监控与严格幂等审计级标准，还差两点：

  1. 把 regretRate 暴露为可观测指标（Micrometer/endpoint/告警源）
  2. 为 idempotencyKey 增加已处理去重闭环（防止 DLQ 重放重复副作用）