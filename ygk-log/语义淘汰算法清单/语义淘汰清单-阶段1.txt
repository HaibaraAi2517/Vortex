E:\1projects\claude\Vortex\ygk-log\语义淘汰清单.txt。这是专家级别的执行清单。请你一步步做，有任何需要告诉我


• 我会先读取这份执行清单，确认里面的步骤、前置条件和交付物，然后按清单顺序推进。接下来先看文件内容，再判断第一步需要改什
  么或运行什么。

• 我先定位这几处实现，确认现状和依赖关系，然后从阶段 1 的第 1 项开始落地。看完代码后我会给出一个精简执行顺序，并直接改第
  一批问题。

• 代码位置已经找到，仓库根目录不是当前目录，我还要确认实际 git 根和相关测试文件。接下来会并行读取阶段 1 涉及的实现与测
  试，先把 1-5 项的最小可行改动范围收敛出来。

• 我已经确认了阶段 1 的代码现状。第一批改动会集中在这几件事：

  1. MemoryFragment：把 recency 从 24h 线性归零改成对数衰减，并补上 importance 动态提升入口。
  2. HierarchicalMemoryController：recall 改为命中后走 get(id) 刷新访问时间；maybeEvict 改成 namespace 内候选，并按释放
  token 目标做稳定淘汰。
  3. CaffeineHotStore / SemanticEvictionPolicy：补 namespace 候选读取能力、按 score/tokenCount 做“价值密度”排序。
  4. 测试：补阶段 1 对应回归用例，先把 1-5 项落到可验证状态。

  我先更新执行计划，然后开始改代码。

• 我在补阶段 1 的实现细节，接下来会先改核心类，再把测试补齐。改动会尽量保持接口最小变化，避免把阶段 2-4 的内容提前耦合进
  来。

• 我在收紧阶段 1 的实现边界。现在看起来第 5 项没有现成的 “answer-grounded callback” 接口，所以我会先把 recall 路径上的动
  态 importance 更新做完整，并把更新入口封装到 MemoryFragment，后续阶段 2/4 再把回答反馈接进去。

• 我在补实现前再扫一遍相关入口，确认没有隐藏的回调或已有 importance 更新链路，避免重复造接口。然后就直接打补丁。

• 我现在开始改文件：先把 MemoryFragment 的时间衰减和 importance 更新能力补上，再改 HMC 的 recall/eviction 流程，最后更新
  测试。改完会马上跑定向测试。

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 核心改动已经打上，我先做一次代码自检，重点看有没有接口漏改、导入缺失，避免直接跑测试才发现机械性编译问题。然后再执行
  Maven 定向测试。

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 有一个机械性问题要先修：HierarchicalMemoryController 里还在用 CaffeineHotStore 做模式匹配，但刚才导入被我删掉了。我先
  补这个小修，再跑定向测试。

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 实现层现在基本齐了，我开始跑阶段 1 相关的 Maven 测试，先覆盖 vortex-common、vortex-storage、vortex-kernel 三个模块的受
  影响用例。若测试暴露设计问题，我会直接修到通过。

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• PowerShell 把 -Dtest 的逗号列表拆坏了。我改成带引号的参数重新跑，同样是定向测试，不会扩大执行范围。

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 阶段 1 的代码和回归测试已经跑通。我现在把结果收束成清晰状态，然后说明下一步进入阶段 2 前你需要确认的一点：第 5 项里的
  answer-grounded callback 目前仓库里没有现成入口，所以这次先把 recall 命中时的 importance 动态更新落地了，回答侧回写要
  么你指定现有链路，要么我下一步补一个新接口。

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 已按清单先完成阶段 1 的可落地部分，并做了定向验证。

  这次已落实的改动：

  - recency 从 24h 线性归零改成对数衰减，超过 24h 仍保留区分力，位置在 /E:/1projects/claude/Vortex/vortex-common/src/
  main/java/com/vortex/common/model/MemoryFragment.java。
  - recall 命中 L1 时改为通过 get(id) 刷新 lastAccessTime，并在召回时动态提升 importance，位置在 /E:/1projects/claude/
  - 淘汰候选改为仅看触发 namespace 内的片段，避免跨 namespace 候选池混用，位置同上。
  - 淘汰策略从“前 10% 条目数”改成按 score/tokenCount 的价值密度排序，并按目标 token 释放量选片段，位置在 /E:/1projects/
  claude/Vortex/vortex-kernel/src/main/java/com/vortex/kernel/hmc/SemanticEvictionPolicy.java。

  - mvn test -pl vortex-common,vortex-storage,vortex-kernel
  "-Dtest=MemoryFragmentTest,CaffeineHotStoreTest,SemanticEvictionPolicyTest,HierarchicalMemoryControllerTest"

  有一个需要你确认的点：

  - 清单第 5 项写的是“在 recall / answer-grounded 回调中更新 importance”。当前仓库里没有现成的 answer-grounded 回写入
  口，所以这次先把 recall 路径做好了。
  - 如果你有现成的“最终回答落地”链路或类名，告诉我位置，我下一步直接把 answer-grounded importance 回写接上。
  - 如果没有，我可以下一步为 HMC 补一个明确接口，例如 reinforceFragments(List<String> fragmentIds, String namespace)，供
  上层在回答完成后调用。

  如果你同意，我下一步继续做阶段 2，从结构化 eviction decision log 开始。