package com.vortex.kernel.hmc;

public enum MemoryPipelineStage {
    ADMISSION,
    L1_WRITE_THROUGH,
    EXTRACTION,
    SUMMARY,
    SPLIT,
    EMBEDDING,
    L1_ADMISSION,
    L2_INDEX,
    L3_ARCHIVE
}
